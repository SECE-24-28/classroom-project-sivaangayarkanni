# Day 3 - HTML + CSS

This folder contains HTML files with separate CSS styling.

## Files:
- HTML files (same as day-2)
- `styles.css` - Common stylesheet with responsive design

The CSS file includes:
- Reset styles
- Common components (cards, buttons, forms)
- Grid layouts
- Responsive design for mobile devices
